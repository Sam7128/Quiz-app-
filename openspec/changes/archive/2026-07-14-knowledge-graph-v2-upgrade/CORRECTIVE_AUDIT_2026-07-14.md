# Corrective Audit — 2026-07-14

## 結論

封存的 `knowledge-graph-v2-upgrade` 已完成其 `tasks.md` 所列範圍，但該範圍並不完整等同使用者最初要求。歷史規格曾明確排除多主題模板及圖片上傳，並把節點黑點互動限定為拖線到空白處的 `DropNodeMenu`。因此，過往「完成 OpenSpec」不應再被描述為「完全符合原始需求」。

## 已確認的規格落差

- 原始需求：節點黑點旁提供小型編輯／形狀／新增／刪除工具列。
  - 封存設計：只定義 `onConnectEnd` 拖至空白後顯示四形狀 DropNodeMenu。
- 原始需求：提供多組配色模板。
  - 封存設計 D9：明確決定只保留經典配色重設，不實作主題切換。
- 原始需求：上傳圖片並自由拖放在節點旁，可隨圖表跨裝置同步。
  - 封存設計 Non-Goals：明確排除 Supabase Storage 上傳，只接受外部 URL。
- 原始需求：自動排版需避免混亂、線條交錯與節點重疊。
  - 封存 tasks：沿用原有簡單 radial 演算法，沒有量化重疊／分支局部性驗收。

## 修正實作清單

- [x] `NodeQuickMenu`：黑點點擊顯示編輯、8 種形狀、快速新增子節點、刪除。
- [x] 自由／放射模式分離：自由可拖曳；放射立即智慧排版並鎖定概念節點。
- [x] 子樹扇區 radial：依連通元件、葉權重、深度與同圈密度排版；保留便利貼／圖片。
- [x] 自連線／懸空連線在載入、程式模式與序列化邊界清除，清理前保留一次原始備份。
- [x] 6 組分支配色模板。
- [x] 最多 4 張安全點陣圖上傳；6 MB 來源上限，壓縮後以受限 WebP data URL 存入 graph JSON 並沿用圖表雲端同步。
- [x] Supabase Auth Web Locks 修正與自訂同步鎖 Promise 回歸測試。
- [x] `tsc`、41 files／261 tests、Vite build、2 個聚焦 Playwright 情境通過。

## 歷史完整性

原 `tasks.md` 維持全數 `[x]`，因其代表當時被核准且已完成的縮限範圍；本附錄記錄後續對原始需求的補正，不回寫歷史內容來掩蓋規格落差。
