# MindSpark 知識圖模組 V2 測試基礎建設規格書 (TEST_INFRA.md)

本文件定義了知識圖模組（Knowledge Graph）V2 升級專案的端對端 (E2E) 測試基礎建設，包含測試策略、Category-Partition 等價類劃分以及 4-Tier 測試案例矩陣。

## 1. 測試環境與隔離策略
- **測試框架**：Playwright (Chromium) + TypeScript。
- **測試入口**：`e2e/knowledge-graph.spec.ts`。
- **資料隔離**：
  - 在每次測試執行 (`beforeEach`) 時，執行 `page.evaluate(() => localStorage.clear())` 確保無殘留資料污染。
  - 在 `localStorage` 中自動寫入 `mindspark_settings` 鍵，設定 `betaFeatures.knowledgeGraph: true` 以開啟實驗功能入口。
- **互動原則**：
  - 使用自製 `ConfirmDialog` 互動方式（點擊對應確認/繼續按鈕），不得使用 native dialog 監聽器。

---

## 2. Category-Partition 測試設計矩陣 (C1 - C9)

| 類別 (Category) | 劃分 / 等價類 (Partitions) | 預期結果 / 驗證點 | 實現狀態 (M0) |
| :--- | :--- | :--- | :--- |
| **C1: 圖表建立與重命名** | 1. 預設名稱建立<br>2. 變更為有效自訂名稱<br>3. 輸入超長名稱 (超出 NAME_MAX 50)<br>4. 輸入純空白名稱 | 1. 建立預設名稱為「新圖表」之圖表<br>2. 正常修改並儲存<br>3. 自動截斷至 50 字元<br>4. 回退為「未命名圖表」 | 🟢 已實現 |
| **C2: 節點新增與限制** | 1. 視覺模式新增概念節點<br>2. 新增便利貼節點<br>3. 節點數量達上限 (MAX_NODES 200)<br>4. 便利貼達上限 (MAX_STICKY_NOTES 20) | 1. 成功新增且位置在中心<br>2. 成功新增黃色便利貼<br>3. 彈出 Toast 警告並禁止新增<br>4. 彈出 Toast 警告並禁止新增 | 🟢 已實現 (限制警告部分已 skip 待 V2) |
| **C3: 節點屬性編輯** | 1. 修改概念標題 (在限值內)<br>2. 修改標題超出上限 (TITLE_MAX 100)<br>3. 切換形狀 (方形/圓角/菱形)<br>4. 變更顏色與字體大小 | 1. 畫布即時更新標題<br>2. 輸入被截斷或彈出錯誤<br>3. 節點形狀正確切換<br>4. 自訂屬性成功套用 | 🟢 已實現 |
| **C4: 連線操作與限制** | 1. 拖曳連線兩個節點<br>2. 連線自身 (自環 self-loop)<br>3. 雙擊修改連線標籤<br>4. 連線標籤超出上限 (EDGE_LABEL_MAX 100)<br>5. 連線數量達上限 (MAX_EDGES 500) | 1. 成功建立單向連線<br>2. 系統阻斷，不建立連線<br>3. 連線顯示對應文字<br>4. 自動截斷或彈出錯誤<br>5. 彈出 Toast 警告並禁止連線 | 🟢 已實現 (自環/超長標籤已 skip 待 V2) |
| **C5: 逐步探索模式** | 1. 切換至逐步探索模式<br>2. 點擊概念節點循環展開 (L1 ➔ L2 ➔ L3 ➔ L1)<br>3. 從 progressive 切回 expand-all<br>4. 再次切回 progressive 重置 | 1. 節點預設收合，僅顯示標題<br>2. 按點擊顯示定義、詳情、折疊<br>3. 所有節點內容完全顯示<br>4. 所有節點 `expandLevel` 應重置為 0 | 🟡 待 V2 實現 (已標記 skip) |
| **C6: 拖曳至空白處新增** | 1. 從節點 Handle 拖曳連線釋放於空白處 | 1. 彈出 `DropNodeMenu` 浮動選單，點選形狀後自動建立節點與連線 | 🟡 待 V2 實現 (已標記 skip) |
| **C7: 雙模式互轉與還原** | 1. 視覺修改樣式 ➔ 切至代碼 ➔ 切回視覺<br>2. 代碼模式修改層級與縮排 ➔ 切回視覺<br>3. 編輯距離 Levenshtein 距離 ≤ 2 的改名匹配樣式保留<br>4. 重命名超出編輯距離 ➔ 祖先路徑失效重置 | 1. 節點樣式 100% 保留<br>2. 視覺樹狀結構與連線正確更新<br>3. 保留子節點樣式與座標<br>4. 子分支樣式與位置重置 | 🟢 基礎雙模式已實現 / 🟡 Ancestor Path 還原待 V2 (已標記 skip) |
| **C8: 富文本筆記管理** | 1. 點擊節點編輯 Tiptap 富文本筆記<br>2. 修改節點標題時筆記鍵值同步級聯變更<br>3. 貼入 Base64 格式圖片安全消毒過濾 | 1. 筆記內容存入 `notes` 字典，支援 autosave<br>2. 筆記鍵值同步變更為新標題<br>3. 自動過濾清除圖片，防範 XSS 與容量爆滿 | 🟢 筆記編輯與級聯更名已實現 / 🟡 安全消毒待 V2 (已標記 skip) |
| **C9: 雲端同步與衝突解決** | 1. 登入用戶編輯 ➔ 斷網 ➔ 聯網自動重試<br>2. 雙向修改衝突 ➔ 選擇「另存新圖表」 | 1. 斷網記錄於 dirty graph，聯網時自動重新同步<br>2. 彈出 ConfirmDialog，點選另存後建立「圖表名稱 (衝突副本)」的新圖表 | 🟡 待 V2 實現 (已標記 skip) |

---

## 3. 4-Tier 測試案例矩陣與設計要求

### **Tier 1: Smoke Tests (最基本的主流程)**
- **KGV2-T1-01: 建立預設名稱圖表並新增節點與連線**
  - **步驟**：新建圖表 ➔ 進入畫布（確認標題為「新圖表」）➔ 新增一個概念節點 ➔ 確認節點成功渲染。
  - **預期結果**：正常建立「新圖表」且畫布上出現標題為「新概念」的節點。

### **Tier 2: Core Features / Boundary Limits (核心功能與邊界限制)**
- **KGV2-T2-01: 圖表命名與重命名邊界測試**
  - **步驟**：在圖表列表雙擊圖表名稱進行重新命名。
  - **測試等價類**：
    - 有效名稱：輸入「我的新圖表」 ➔ 成功儲存。
    - 超長名稱：輸入超過 50 個字元的超長名稱 ➔ 應自動截斷至 50 個字元。
    - 空白名稱：輸入純空格 ➔ 應退回為「未命名圖表」。
- **KGV2-T2-02: 節點屬性編輯測試**
  - **步驟**：選擇概念節點 ➔ 開啟右側「編輯節點」面板 ➔ 修改標題 ➔ 切換形狀（方形/圓角/菱形） ➔ 變更字體大小（小/中/大）。
  - **預期結果**：屬性即時或 debounce 後套用至畫布，標題修改超過 100 字元時應自動截斷。
- **KGV2-T2-03: 便利貼新增與編輯測試**
  - **步驟**：點擊「新增便利貼」 ➔ 雙擊便利貼進入編輯模式 ➔ 輸入文字 ➔ 按 Enter 保存。
  - **預期結果**：成功新增黃色便利貼並寫入內容，文字若超長則截斷。
- **KGV2-T2-04: 連線建立與自環限制測試**
  - **步驟**：在視覺模式下，將節點連線。
  - **預期結果**：成功建立單向箭頭連線。自環（自己連自己）應被系統阻斷，測試在 M0 中已標記 `skip`，待 V2 完成後啟用。
- **KGV2-T2-05: 節點與便利貼數量上限測試**
  - **預期結果**：圖表數量上限 (20 張)、節點上限 (200 個)、便利貼上限 (20 個)、連線上線 (500 條)。當觸碰上限時，系統彈出 Toast 提示。此測試在 M0 中已標記 `skip`。

### **Tier 3: Advanced Integrations / State Restores (進階整合與狀態還原)**
- **KGV2-T3-01: 逐步探索模式與狀態重置測試**
  - **步驟**：切換至「逐步探索模式」 ➔ 點擊概念節點循環展開 (L1 ➔ L2 ➔ L3 ➔ L1) ➔ 切回全展開 ➔ 再切回逐步探索。
  - **預期結果**：循環顯示節點的標題、定義、詳情。再次切回逐步探索模式時，`expandLevel` 應正確重置為 0。此測試在 M0 中已標記 `skip`。
- **KGV2-T3-02: 雙模式轉換與樣式/結構保留測試**
  - **步驟**：在視覺模式修改節點顏色/形狀 ➔ 切換至代碼模式（確認 Markdown 代碼生成）➔ 切回視覺模式。
  - **預期結果**：節點的樣式與結構無縫保留。代碼模式下修改縮排，切回視覺模式應生成正確的樹狀結構與連線。
- **KGV2-T3-03: 雙模式 Ancestor Path 還原與 Levenshtein 模糊匹配測試**
  - **步驟**：代碼模式下手動微調父節點名稱（編輯距離 <= 2）或重命名超出距離（> 2）。
  - **預期結果**：距離 <= 2 時，能透過 Heuristic 保留其子節點的樣式與座標；距離 > 2 時祖先路徑失效樣式重置。此測試在 M0 中已標記 `skip`。
- **KGV2-T3-04: Tiptap 筆記編輯級聯更名與 XSS 消毒測試**
  - **步驟**：選取節點 ➔ 開啟富文本筆記 ➔ 輸入筆記 ➔ 變更該節點標題 ➔ 檢查筆記字典鍵值是否同步級聯變更。貼入帶有 XSS script 的圖片或 Base64 格式。
  - **預期結果**：筆記成功儲存，更名時級聯同步。圖片過濾消毒已標記 `skip`，待 V2 完成後啟用。

### **Tier 4: Edge Cases / Sync Conflicts / Network Resilience (異常邊界與網路重試)**
- **KGV2-T4-01: 拖曳連線至空白處彈出 DropNodeMenu 測試**
  - **步驟**：自節點 Handle 拖曳出連線釋放於空白畫布。
  - **預期結果**：彈出 DropNodeMenu 浮動選單，點選形狀後自動建立新節點與邊。此測試在 M0 中已標記 `skip`。
- **KGV2-T4-02: Supabase 同步與斷網離線 LWW 自動重試測試**
  - **步驟**：登入帳號 ➔ 編輯圖表 ➔ 斷網（網路離線）➔ 聯網（網路在線）。
  - **預期結果**：離線時將 dirty Fallback 寫入本地，聯網後自動觸發重新上傳同步。此測試在 M0 中已標記 `skip`。
- **KGV2-T4-03: Supabase 雙向修改衝突另存新圖表測試**
  - **步驟**：模擬本地與雲端 `updatedAt` 不一致 ➔ 觸發同步衝突 ➔ 彈出自製 `ConfirmDialog` ➔ 點選「另存為新圖表」。
  - **預期結果**：在自製 Dialog 中點擊另存按鈕，成功建立「圖表名稱 (衝突副本)」的新圖表。此測試在 M0 中已標記 `skip`。
